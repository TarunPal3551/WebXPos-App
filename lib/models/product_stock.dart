class ProductStock {
  String productName;
  int productId;
  num currentStock;
  num soldStock;
  num soldSaleValue;

  ProductStock(this.productName, this.currentStock, this.soldStock,
      this.productId, this.soldSaleValue);
}
